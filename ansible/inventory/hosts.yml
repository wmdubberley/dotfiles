# Example static inventory — bootstrap.sh generates a temp inventory at runtime.
# Add permanent machines here once they are bootstrapped.
all:
  children:
    workstations:
      hosts:
        # main-desktop:
        #   ansible_host: 192.168.x.x
        #   ansible_user: william
    servers:
      hosts:
        # home-server:
        #   ansible_host: 192.168.x.x
        #   ansible_user: william
    minimal:
      hosts:
        # travel-laptop:
        #   ansible_host: 192.168.x.x
        #   ansible_user: william
